package selenium.ObjectRepository.com;

import org.openqa.selenium.WebDriver;

public class PageURL  {
 
	public void visitURL(String data, WebDriver getDriver) {
		getDriver.get(data);	
	}
	
}
